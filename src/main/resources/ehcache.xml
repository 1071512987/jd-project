<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xsi:schemaLocation="http://www.ehcache.org/v3
        http://www.ehcache.org/schema/ehcache-core.xsd">
<!--    <persistence directory="F:/ehcache"/>-->

    <!--
        alias代表别名，expiry设置有效期，none代表永不过期
        ttl设置固定生命周期(time to live)：中途登录不更新周期 unit设置单位
        tti设置闲置生命周期(time to idle)：中途登录更新周期
        key-type 代表key值类型，value-type 代表value值类型
        resource代表资源存放位置，
        heap代表堆内存，entries代表以数量计算（默认），unit代表以大小来计算，缓存采用淘汰算法
        offheap代表堆外内存，其只能按大小unit计算
        disk代表磁盘，persistent=true必须设置持久化，且disk缓存必须与persistence标签配合使用，代表磁盘位置
        公共的代码内容可以放入cache-template中，并配置相应的名字，配置cache时通过uses-template标签声明使用哪一个template
    -->

    <cache-template name="common">
        <key-type>java.lang.Object</key-type>
        <value-type>java.lang.Object</value-type>
        <resources>
            <heap>10000</heap>
<!--            <offheap unit="MB">50</offheap>-->
<!--            <disk unit="GB" persistent="true">1</disk>-->
        </resources>
    </cache-template>

    <!-- 存放token的缓存：只要7天内不访问，就失效 -->
    <cache alias="token" uses-template="common">
        <expiry>
            <tti unit="days">7</tti>
<!--            <tti unit="seconds">5</tti>-->
        </expiry>
        <listeners>
            <listener>
                <class>com.hzau.college.common.cache.TokenCacheListener</class>
                <!-- 异步回调 -->
                <event-firing-mode>ASYNCHRONOUS</event-firing-mode>
                <!-- 不用按顺序处理事件 -->
                <event-ordering-mode>UNORDERED</event-ordering-mode>
                <!-- 哪些操作会触发监听器：添加、过期、删除 -->
                <events-to-fire-on>CREATED</events-to-fire-on>
                <events-to-fire-on>EXPIRED</events-to-fire-on>
                <events-to-fire-on>REMOVED</events-to-fire-on>
            </listener>
        </listeners>
    </cache>

    <!-- 默认缓存：永不过期 -->
    <cache alias="default" uses-template="common">
        <expiry>
            <none/>
        </expiry>
    </cache>
</config>